[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
markers =
    integration: needs a real postgres and kafka (docker required)
filterwarnings =
    ignore::DeprecationWarning
